# 项目全局约定

本文件适用于整个仓库。后续功能、文案、数值、存档和测试都必须遵循以下已确认的产品规则。

## 统一羁绊成长（2026-09-23）

- 每个角色只有一份全局「羁绊分」，正传、卡册、角色日常、投喂和共同演奏共用；跨章节、重开章节、刷新保留。
- 不再独立累计心动值、好感度、信任度或演奏羁绊。同类关系称呼只可作为叙事表达。团宠关系同样使用羁绊分；不同角色/不同猫咪不能混为一人。
- 羁绊是长期成长，不是快速刷满的奖励。普通投喂统一消耗 10 音符、增加 1 分，每角色每天最多 3 次；所有投喂都必须付出音符，不允许免费礼物或倍增羁绊绕过规则。用户确认的特殊投喂「满心礼盒」为例外：卡册角色消耗 10000 音符，羁绊直接提升到 100，不占用、不受每日三次限制；余额不足或羁绊已达 100 时不扣款，不倒扣超过 100 的历史值。
- 音符为唯一消费货币，正传和卡册共用。取消正传体力、独立团费与恢复体力操作；战斗回合状态、猫咪精力、角色合奏精力保持各自用途。
- 普通剧情互动增加 1～2 分；明确支持、维护、陪伴或选择某位角色的关键选项可为对应角色增加 5 分。奖励需在代码中显式配置，不能凭文字自动推断；剧情选项不展示羁绊加分、受益角色或「奖励已领取」提示，避免用收益引导选择。实际结算与去重规则保留。
- 同一剧情奖励只结算一次，重读、重开章节、重复点击不能刷取。聊天、演奏、技能陪伴等可重复互动每角色每天合计最多增加 1 分；与投喂的 3 次额度分开。
- 35 分是所有隐藏技能/隐藏档案的最低开启门槛，界面和实际执行入口均需校验。已有编队、形态、专属事件等附加条件保留并明确展示。
- 普通培养以约 7～10 个活跃日到达 35 分为参考，关键剧情可以提前达到，不设强制等待天数。
- 羁绊分表达关系积累；不因正常投喂或羁绊增长自动触发坏结局，剧情风险使用明确的关键选择。
- 普通羁绊成长沿用 100 分上限；旧正传存档中超过 100 的有效数值保留，不倒扣。
- 心情、精力、饱腹、琴技、角色等级等用途不同的状态可保留；不要再为羁绊增设独立经验条。
- 旧存档按同一角色的最高有效关系值合并，不直接相加，不清空已有成长和已收藏内容；迁移必须幂等。历史心动值不能分配成任意角色的羁绊。
- 调整规则时同步修改帮助文案、奖励提示、解锁条件与回归测试，重点验证余额不足、每日上限、剧情去重、35 分边界、跨页面同步和存档迁移。

## 音符经济（2026-09-24）

- 新档 30 音符；一次性礼包 10；角色故事首次完成 3；每日任务 10，邀请券仍为 1 张。旧档已有音符不扣减，不把退役团费兑换成音符。
- 正传普通练琴和延期加练均消耗 10 音符，琴技 +2；不足时禁止扣款、增长与延期，始终可免费进入节奏演奏赚钱。
- 完整节奏演奏达到 C（准确率至少 45%，且有手动命中）才发音符。C/B/A/S 基础奖励按曲目和难度区分：其他曲目简单 3/4/5/6、困难 4/5/6/7；《恋与哈基米（OP）》简单 5/6/7/8、困难 7/8/9/10（2026-09-26 用户调整）。编队和主动技能合计最多另 +2。每天前 3 场达标正常结算，第 4 场起每场总计 1 音符；中断或未达标不占额度、不发音符。
- 每章首次完成奖励 15 音符、1 张邀请券。其他结局仍收录回忆，但不叠加货币与券；重开、换结局、刷新均不得重置奖励记录。
- 第二、四章赞助节点各首次 +5 音符；柠檬借款消耗 20 音符且不得透支。考核免费，合奏依羁绊判定，不加琴技；首次考核额外 +3，每日首次考核通过 +3。阿喆安可每日首次 +2，之后仅经验与纪念；首席四拍练习每日首次 +5 保留。
- 剧情琴技奖励每章每节点只一次，普通练琴之外不可反复免费刷琴技。永久领取记录和每日计数在全局保存，迁移根据可确认的旧剧情、结局记录补齐，不重发已知奖励。
- 普通活跃日约 25～35 音符为调参参考，不是硬性每日总上限；7～10 日到 35 羁绊以重点培养一位角色为参考，多角色同时培养更慢。
- 同步收入说明、技能加成上限、余额不足提示和测试，保留剧情与非货币角色技能效果。

## 开发与验证

- `index.html` 仅保留页面结构与资源入口。代码按 `js/data`（配置）、`js/core`（存档/经济/羁绊）、`js/chronicle`（正传）、`js/cards`（卡册与技能）、`js/features`（独立玩法）、`js/audio`、`js/ui`、`js/app` 划分；样式位于 `styles/`。修改入口与依赖说明见 `docs/architecture.md`。
- 不把大段业务逻辑或样式重新塞回 HTML。角色目录每角色只定义一次；规则以 core 中的统一入口为准。
- 保留静态部署和直接打开 HTML 的能力。经典脚本按依赖顺序 `defer` 加载，禁止 `async` 打乱顺序；读档、事件绑定与首屏渲染由最后的 `js/app/bootstrap.js` 统一启动。
- 任何 `js/` 文件有改动，都必须在提交或发布前运行 `node scripts/version-assets.cjs`，同步更新 `index.html` 中该文件对应的 `?v=内容哈希`；`styles/` 文件有改动时同样处理。不得手工复用旧版本号或漏传更新后的 `index.html`。运行 `node scripts/version-assets.cjs --check` 验证版本号与文件内容一致；`tests/architecture.cjs` 也必须覆盖此检查。
- 正传模块通过显式工厂和 `ctx` 协作；跨模块访问当前状态，不缓存可能在重开/导入时替换的存档对象。样式链接顺序保留现有覆盖关系。
- 浏览器回归测试位于 `tests/`，使用 Node.js 与 Playwright；运行方式见 `tests/README.md`。结构调整至少检查 architecture、经济/羁绊和相关功能回归；存档键与旧档兼容不可因分文件改变。
- 不覆盖与当前需求无关的用户修改或本地配置。

## 连贯剧情、每周训练与插图（2026-09-24 修订）

- 每章先连贯讲完前段主要剧情，再进入五周训练与相处，第六周正式演出。不要把开场拆成每周几句；临近演出的事件保留原有时间关系（如第三章第 4 周心事、第 5 周鼓位、第四章第 5 周赞助）。
- 训练周不再要求每周读一段主线。先完成已开放的待续剧情即可推进时间；训练可按需参加，已开放的项目允许后来补练。旧档从原节点继续，保留周数、选择与已读记录，不强制重读开场。
- 每章五项训练：视听练耳、旋律记忆、霓虹节奏、搭档接奏、舞台彩排；随章节更换主题、旋律和难度。游戏需要真实操作，不能只有按钮改名或直接发奖。
- 视听练耳替代第一项节拍跟练：C 大调两小节简谱，播放时逐拍高亮但不揭示缺音，听完用 1–7 填空、可修改和免费重听，提交后按缺音正确率评分，以原简谱布局展示正确音和错音对照，可逐拍高亮播放正确旋律复核，试听不重计分、不重复领奖；六章各三轮题目，逐步增加缺音和跳进。静音或音频不可用时提示开声重试；切页/后台停止音频。保留原报名、轮次与领奖记录，舞台彩排第一轮继续跟拍。
- 旋律记忆每轮示范并复现 10 个音，独立训练和舞台彩排的记旋律环节共用；保留四个音高按键，输入满 10 个音后按正确位置数量评分（每音 10 分），点错继续，不提前结束；输入进度不代表正确数，每轮重新计数。
- 霓虹节奏替代静态找不同：四轨下落音符，D/F/J/K 或多点触控，按击打时机计分、漏拍断连、空拍扣分；逐章逐轮加速，后续章节加入双键和弦。舞台彩排第三轮使用同一玩法。音画共用时间轴，后台/切页暂停、重试不重复收费；不改旧报名与已领奖记录。
- 每章每项首次报名 10 音符，三轮平均 60 分完成后琴技 +2，90 分额外 +1（总计 +3）；首次完成结算一次。失败、中断、切页、刷新和重开后的续练免费；已领奖项目可免费温习，不重复发奖、不补发优秀差额。
- 训练报名与收益记录保存在全局经济数据，尝试进度保存在章节数据。重开恢复章节起点时保留本章训练已学到的琴技，不退报名费、不重发成长。原普通练琴和延期加练继续消耗 10 音符、增加 2 琴技。
- 初次空格考核为入团摸底，失败同样继续剧情与训练；后续为阶段验收，仍不增加琴技。演出门槛明确显示差值和补练入口；训练期间背景音乐暂停，听觉提示必须有同步视觉提示，切页/后台暂停计时。
- 原有羁绊、结局、角色分支与奖励键保留；训练不额外增加音符、羁绊或新资源条。常规训练路径应足以达到演出琴技门槛。
- `js/data/chronicle-weeks.js` 保留剧情块目录供连贯路由与旧档迁移；`js/chronicle/weeks.js` 管理开场衔接、晚期事件和周进度。训练配置在 `js/data/chronicle-training.js`，玩法与报名在 `js/chronicle/training.js`，样式在 `styles/chronicle-training.css`。
- 场景配图在 `js/data/chronicle-art.js` 和 `js/chronicle/art.js`，按实际剧情节点绑定，不依赖旧版每周主线划分。
- 正传阅读采用与「心动故事」一致的场景画面、紧凑对白和选项布局。阅读时收起章节大横幅，羁绊与目标放在可展开面板；不要再用大白卡左右两栏强行等高。
- 场景图保留完整构图，以有限尺寸的照片呈现，并以同图模糊背景营造氛围。桌面端标题、对白、选项在同一列连续排列，照片放在旁边并与对白框垂直居中，不让标题与照片独占一大排；对白按内容自然撑高，不与图片强制等高。移动端上下排列。无专属插图的节点只显示紧凑对白，不继承无关排练图、不留空画框。
- 插图按「章节 + 场景 + 必要分支条件」显式绑定；成功/失败、低等级擦肩而过/争执、提前结局等须与实际对白匹配。新增图片应保持角色外观与乐器一致，不能反复拿同一张群像配不同事件。
- 到达并展示有插图的场景即自动收藏到回忆相册；剧情、相册、详情和下载使用同一图片。只收藏实际走到的分支，不按周数批量解锁，不发额外音符或羁绊。刷新与重开保留收藏且不重复。
- 旧档只根据真实已读手记补齐可确认的场景；无法确认成功/失败等条件的旧记录不推测补图。原回忆 ID 和隐藏技能门槛保持。
- 用户提供的山丘环境与朱老师吧台图保留在入团、酒单对应节点；排练原图保留为参考素材。吧台调酒的人为朱老师。

## 相册配图

- 进入章节结束页时自动展示对应结局纪念图，保持显示直到玩家手动关闭，不设置自动关闭计时器；每次章节周目只自动展示一次，记录随存档保留，刷新和切页不重复弹出。重开章节可再次展示；相册主动查看保持手动关闭。

- 回忆插图按具体事件与结局匹配，不用角色立绘替代考核、合约、停业或演出结果；成功、失败和取消场景的情绪应可区分。
- 相册缩略图、详情与下载共用 `MEMORIES.asset`，完整保留构图；换图不改回忆 ID、收藏存档或隐藏解锁条件。
- 8 合 1 原图与提示词、裁切坐标必须留存。原相册素材在 `assets/album/source/`，用 `scripts/crop-album-art.py` 重建；正传场景在 `assets/chronicle/scenes/source/`，用 `scripts/crop-chronicle-art.py` 重建。索引与提示词位于 `docs/imagegen/`，逐格检查图文与角色对应。

## 礼品卡

- 首页礼物入口支持本地签发的音符礼品卡；ECDSA P-256 / SHA-256 私钥只留在网站目录之外，前端只含公钥，禁止内置私钥或跳过验签。
- 礼品卡金额、编号、用途及有效期均需验签和校验；按卡编号在全局经济存档记录核销，与音符一次保存，失败回滚，不重复发奖。见面礼领取状态不影响礼品卡入口。
- 本版仅保障保留记录的本地存档去重，不能宣称跨设备或回滚存档后全局一次性；严格核销需要服务端。工具与操作说明见 `docs/gift-cards.md`。

## 个人线人物形象（2026-09-25）

- 冰冰是女性大提琴手，外形精致亮眼，具有女明星般的气质；不得画成男性。
- 雪子是男性；对白归属、角色说明与场景插图必须保持一致。

## 第七章个人线（2026-09-25）

- 完成第六章后，弹窗选择角色进入第七章。对应角色全局羁绊必须严格大于 35（至少 36），界面和实际入口均校验；不改变其他隐藏技能的 35 分门槛。
- 阿喆和十元个人线分别保存剧情、选择、已读与结局；切换、刷新保留，重读不重置全局羁绊、相册及奖励。不同个人线共用第七章首次结局奖励键。
- 每个个人线阅读节点使用独立场景图；只收藏真正读到的分支。8 合 1 原图在 `assets/chronicle/personal/source/`，提示词与裁切坐标在 `docs/imagegen/personal/`，通过 `scripts/crop-personal-art.py` 重建。

## 节奏舞台 OP 完整版（2026-09-26）

- 《恋与哈基米（OP）》在选曲列表首位并默认选中；底层旧曲索引和成绩键保持兼容，不因展示排序迁移或覆盖旧成绩。
- 用户已确认扩充全曲：从原曲 00:00 播放至完整尾声（约 154.85 秒）；开头 2.5 秒原曲用于音乐入场，随后为全曲击打谱面。两档保持原曲速度，困难谱音符更密，奖励高于简单谱。
- OP 按用户要求以鼓点编谱：从 MP3 提取打击乐瞬态、逐段核对节拍并吸附实际鼓点；简单档主要鼓点，困难档加入清晰的踩镲等分拍。MIDI 仅保留作时间/素材参考，不用旋律音符代替鼓点；不把 MIDI 文件头的 120 BPM 当作歌曲实际节拍重排。原 MIDI 在 `assets/audio/rhythm/source/`，生成脚本 `scripts/build-op-drum-chart.py`，校准报告 `docs/rhythm-op-drum-alignment.json`。

- 节奏舞台倒计时 3 秒期间不显示下落音符，3/2/1 各响一次提示音（遵循总静音），暂停/恢复和重复渲染不得补响或重复响；倒计时结束后再入场；OP 此时从 00:00 播放原曲，开头 2.5 秒用于音乐入场，不用静音垫片，首拍要有完整下落距离。音画共用音乐时间，暂停/恢复不得把入场阶段重新当成倒计时；全曲演奏时长和奖励不因准备时间改变。

- 全曲鼓点谱的最佳成绩使用 `love-hakimi-op-full-drums-v1`；此前开头鼓点试玩 `love-hakimi-op-opening-drums-v1`、开头 MIDI 试玩 `love-hakimi-op-opening-v1` 和中段试玩 `love-hakimi-op-preview-v1` 成绩继续保存在旧键，不与新谱面比较或清空。节拍使用局部拍速跟踪，并以短窗起音检测校准落点，不能以单一固定 BPM 延伸整曲。
