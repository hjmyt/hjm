# 项目全局约定

本文件适用于整个仓库。后续功能、文案、数值、存档和测试都必须遵循以下已确认的产品规则。

## 统一羁绊成长（2026-09-23）

- 每个角色只有一份全局「羁绊分」，正传、卡册、角色日常、投喂和共同演奏共用；跨章节、重开章节、刷新保留。
- 不再独立累计心动值、好感度、信任度或演奏羁绊。同类关系称呼只可作为叙事表达。团宠关系同样使用羁绊分；不同角色/不同猫咪不能混为一人。
- 羁绊是长期成长，不是快速刷满的奖励。普通投喂统一消耗 10 音符、增加 1 分，每角色每天最多 3 次；所有投喂都必须付出音符，不允许免费礼物或倍增羁绊绕过规则。
- 音符为唯一消费货币，正传和卡册共用。取消正传体力、独立团费与恢复体力操作；战斗回合状态、猫咪精力、角色合奏精力保持各自用途。
- 普通剧情互动增加 1～2 分；明确支持、维护、陪伴或选择某位角色的关键选项可为对应角色增加 5 分。需显式配置并向玩家展示，不能凭文字自动推断奖励。
- 同一剧情奖励只结算一次，重读、重开章节、重复点击不能刷取。聊天、演奏、技能陪伴等可重复互动每角色每天合计最多增加 1 分；与投喂的 3 次额度分开。
- 35 分是所有隐藏技能/隐藏档案的最低开启门槛，界面和实际执行入口均需校验。已有编队、形态、专属事件等附加条件保留并明确展示。
- 普通培养以约 7～10 个活跃日到达 35 分为参考，关键剧情可以提前达到，不设强制等待天数。
- 羁绊分表达关系积累；不因正常投喂或羁绊增长自动触发坏结局，剧情风险使用明确的关键选择。
- 普通羁绊成长沿用 100 分上限；旧正传存档中超过 100 的有效数值保留，不倒扣。
- 心情、精力、饱腹、琴技、角色等级等用途不同的状态可保留；不要再为羁绊增设独立经验条。
- 旧存档按同一角色的最高有效关系值合并，不直接相加，不清空已有成长和已收藏内容；迁移必须幂等。历史心动值不能分配成任意角色的羁绊。
- 调整规则时同步修改帮助文案、奖励提示、解锁条件与回归测试，重点验证余额不足、每日上限、剧情去重、35 分边界、跨页面同步和存档迁移。

## 音符经济（2026-09-24）

- 新档 30 音符；一次性礼包 10；角色故事首次完成 3；每日任务 10，邀请券仍为 1 张。旧档已有音符不扣减，不把退役团费兑换成音符。
- 正传普通练琴和延期加练均消耗 10 音符，琴技 +2；不足时禁止扣款、增长与延期，始终可免费进入节奏演奏赚钱。
- 完整节奏演奏达到 C（准确率至少 45%，且有手动命中）才发音符。C/B/A/S 基础奖励分别 3/4/5/6；编队和主动技能合计最多 +2。每天前 3 场达标正常结算，第 4 场起每场总计 1 音符；中断或未达标不占额度、不发音符。
- 每章首次完成奖励 15 音符、1 张邀请券。其他结局仍收录回忆，但不叠加货币与券；重开、换结局、刷新均不得重置奖励记录。
- 第二、四章赞助节点各首次 +5 音符；柠檬借款消耗 20 音符且不得透支。考核免费，合奏依羁绊判定，不加琴技；首次考核额外 +3，每日首次考核通过 +3。阿喆安可每日首次 +2，之后仅经验与纪念；首席四拍练习每日首次 +5 保留。
- 剧情琴技奖励每章每节点只一次，普通练琴之外不可反复免费刷琴技。永久领取记录和每日计数在全局保存，迁移根据可确认的旧剧情、结局记录补齐，不重发已知奖励。
- 普通活跃日约 25～35 音符为调参参考，不是硬性每日总上限；7～10 日到 35 羁绊以重点培养一位角色为参考，多角色同时培养更慢。
- 同步收入说明、技能加成上限、余额不足提示和测试，保留剧情与非货币角色技能效果。

## 开发与验证

- `index.html` 仅保留页面结构与资源入口。代码按 `js/data`（配置）、`js/core`（存档/经济/羁绊）、`js/chronicle`（正传）、`js/cards`（卡册与技能）、`js/features`（独立玩法）、`js/audio`、`js/ui`、`js/app` 划分；样式位于 `styles/`。修改入口与依赖说明见 `docs/architecture.md`。
- 不把大段业务逻辑或样式重新塞回 HTML。角色目录每角色只定义一次；规则以 core 中的统一入口为准。
- 保留静态部署和直接打开 HTML 的能力。经典脚本按依赖顺序 `defer` 加载，禁止 `async` 打乱顺序；读档、事件绑定与首屏渲染由最后的 `js/app/bootstrap.js` 统一启动。
- 正传模块通过显式工厂和 `ctx` 协作；跨模块访问当前状态，不缓存可能在重开/导入时替换的存档对象。样式链接顺序保留现有覆盖关系。
- 浏览器回归测试位于 `tests/`，使用 Node.js 与 Playwright；运行方式见 `tests/README.md`。结构调整至少检查 architecture、经济/羁绊和相关功能回归；存档键与旧档兼容不可因分文件改变。
- 不覆盖与当前需求无关的用户修改或本地配置。

## 每周剧情与插图（2026-09-24）

- 六章按周分配既有剧情，每周有明确主线、选择结果和下周预告；不再先读完整章，再连续点击五次空白周数。
- 点击「前往下一周」后直接进入新一周主线，不再要求二次点击「开始本周故事」；周初插曲结束后自动衔接主线，第六周直接进入演出剧情。旧档已有未开始主线的安排页保留继续入口。
- 完成本周主线才可进入下一周；练琴、聊天和可选相遇不自动推进时间，也不要求清空全部支线。第六周开放演出，原有琴技门槛、延期加练和提前结局保留。
- 原有人物对白、选择分支、结局和奖励键尽量保留；新增衔接剧情不自动赠送音符或大额羁绊，不新增体力、团费或准备度资源条。
- 周计划在 `js/data/chronicle-weeks.js` 配置，推进与周存档清理在 `js/chronicle/weeks.js`，场景配图在 `js/data/chronicle-art.js` 和 `js/chronicle/art.js`；周进度随章节分别保存，重开只清本章周进度，全局奖励领取记录不重置。
- 旧存档保留当前剧情和已读选择，不强制从第一周重读；迁移可提升当前对白所在周次，不回退玩家进度。迁移必须幂等。
- 正传阅读采用与「心动故事」一致的场景画面、紧凑对白和选项布局。阅读时收起章节大横幅，羁绊与目标放在可展开面板；不要再用大白卡左右两栏强行等高。
- 场景图保留完整构图，以有限尺寸的照片呈现，并以同图模糊背景营造氛围；对白下紧接选项，移动端上下排列。无专属插图的节点只显示紧凑对白，不继承无关排练图、不留空画框。
- 插图按「章节 + 场景 + 必要分支条件」显式绑定；成功/失败、低等级擦肩而过/争执、提前结局等须与实际对白匹配。新增图片应保持角色外观与乐器一致，不能反复拿同一张群像配不同事件。
- 到达并展示有插图的场景即自动收藏到回忆相册；剧情、相册、详情和下载使用同一图片。只收藏实际走到的分支，不按周数批量解锁，不发额外音符或羁绊。刷新与重开保留收藏且不重复。
- 旧档只根据真实已读手记补齐可确认的场景；无法确认成功/失败等条件的旧记录不推测补图。原回忆 ID 和隐藏技能门槛保持。
- 用户提供的山丘环境与朱老师吧台图保留在入团、酒单对应节点；排练原图保留为参考素材。吧台调酒的人为朱老师。

## 相册配图

- 回忆插图按具体事件与结局匹配，不用角色立绘替代考核、合约、停业或演出结果；成功、失败和取消场景的情绪应可区分。
- 相册缩略图、详情与下载共用 `MEMORIES.asset`，完整保留构图；换图不改回忆 ID、收藏存档或隐藏解锁条件。
- 8 合 1 原图与提示词、裁切坐标必须留存。原相册素材在 `assets/album/source/`，用 `scripts/crop-album-art.py` 重建；正传场景在 `assets/chronicle/scenes/source/`，用 `scripts/crop-chronicle-art.py` 重建。索引与提示词位于 `docs/imagegen/`，逐格检查图文与角色对应。
